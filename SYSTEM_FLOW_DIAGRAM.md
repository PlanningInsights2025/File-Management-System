# 📊 Complete System Flow Diagram

## 🔐 How User Login Works

### Step-by-Step Process:

```
┌─────────────────────────────────────────────────────────────────────┐
│                     USER OPENS LOGIN PAGE                           │
│                  (frontend/pages/login.html)                        │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  USER ENTERS CREDENTIALS                                            │
│  Email: admin@fms.com                                              │
│  Password: admin123                                                │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  FRONTEND CALLS API (api.js)                                       │
│                                                                     │
│  authAPI.login(email, password)                                    │
│     │                                                               │
│     └──> POST http://localhost:5000/api/auth/login                │
│          Body: { "email": "admin@fms.com",                        │
│                  "password": "admin123" }                          │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  BACKEND RECEIVES REQUEST (auth.controller.js)                      │
│                                                                     │
│  1. Validate input data                                            │
│  2. Find user in database                                          │
│      SQL: SELECT * FROM users WHERE email = 'admin@fms.com'       │
│                                                                     │
│  3. Compare password                                               │
│      bcrypt.compare('admin123', stored_hashed_password)           │
│                                                                     │
│  4. If valid, generate JWT token                                  │
│      jwt.sign({ id, role }, SECRET_KEY)                           │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  DATABASE RESPONSE                                                  │
│                                                                     │
│  User found: {                                                     │
│    id: 1,                                                          │
│    username: "Admin User",                                         │
│    email: "admin@fms.com",                                        │
│    password: "$2a$10$hashed...",                                  │
│    role: "admin"                                                   │
│  }                                                                 │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  BACKEND SENDS RESPONSE                                             │
│                                                                     │
│  {                                                                  │
│    "message": "Login successful",                                  │
│    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",          │
│    "user": {                                                        │
│      "id": 1,                                                       │
│      "username": "Admin User",                                      │
│      "email": "admin@fms.com",                                     │
│      "role": "admin"                                                │
│    }                                                                │
│  }                                                                  │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  FRONTEND STORES DATA (auth-utils.js)                              │
│                                                                     │
│  localStorage.setItem('token', token)                              │
│  localStorage.setItem('user', JSON.stringify(user))                │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  REDIRECT TO DASHBOARD                                              │
│                                                                     │
│  if (role === 'admin')                                             │
│    window.location.href = 'admin.html'                             │
│  else                                                               │
│    window.location.href = 'dashboard.html'                         │
└─────────────────────────────────────────────────────────────────────┘
```

## 🔄 How Data Fetching Works

### Getting User Profile (Protected Route):

```
┌─────────────────────────────────────────────────────────────────────┐
│  USER CLICKS "GET PROFILE" BUTTON                                   │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  FRONTEND CALLS API                                                 │
│                                                                     │
│  authAPI.getProfile()                                              │
│     │                                                               │
│     └──> GET http://localhost:5000/api/auth/profile                │
│          Headers: {                                                 │
│            'Authorization': 'Bearer eyJhbG...'                     │
│          }                                                          │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  BACKEND AUTH MIDDLEWARE (auth.js)                                  │
│                                                                     │
│  1. Extract token from Authorization header                        │
│  2. Verify JWT token                                               │
│      jwt.verify(token, SECRET_KEY)                                 │
│  3. Decode token to get user ID                                    │
│  4. Fetch user from database                                       │
│  5. Attach user to request: req.user                               │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  BACKEND CONTROLLER PROCESSES REQUEST                               │
│                                                                     │
│  User.findById(req.user.id)                                        │
│  SQL: SELECT * FROM users WHERE id = 1                             │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  BACKEND SENDS USER DATA                                            │
│                                                                     │
│  {                                                                  │
│    "user": {                                                        │
│      "id": 1,                                                       │
│      "username": "Admin User",                                      │
│      "email": "admin@fms.com",                                     │
│      "role": "admin",                                               │
│      "company_id": null,                                            │
│      "created_at": "2026-01-03T10:00:00Z"                          │
│    }                                                                │
│  }                                                                  │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│  FRONTEND DISPLAYS DATA                                             │
│                                                                     │
│  console.log(profile)                                              │
│  // Update UI with user information                                │
└─────────────────────────────────────────────────────────────────────┘
```

## 💾 Where Data is Stored

### Browser (Frontend)
```
localStorage
├── token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6MSwicm9sZSI6ImFkbWluIiwiaWF0IjoxNjczNTQ..."
└── user: '{"id":1,"username":"Admin User","email":"admin@fms.com","role":"admin"}'

sessionStorage (not used currently)

Cookies (not used currently)
```

### MySQL Database (Backend)
```sql
-- users table
+----+--------------+------------------+----------+-------+------------+---------------------+---------------------+
| id | username     | email            | password | role  | company_id | created_at          | updated_at          |
+----+--------------+------------------+----------+-------+------------+---------------------+---------------------+
| 1  | Admin User   | admin@fms.com    | $2a$10.. | admin | NULL       | 2026-01-03 10:00:00 | 2026-01-03 10:00:00 |
| 2  | Test User    | user@fms.com     | $2a$10.. | user  | NULL       | 2026-01-03 10:00:00 | 2026-01-03 10:00:00 |
+----+--------------+------------------+----------+-------+------------+---------------------+---------------------+
```

## 🔒 Security Features

### Password Hashing
```
Plain Password: "admin123"
        │
        ▼
bcrypt.hash(password, 10)
        │
        ▼
Stored in DB: "$2a$10$rQXZ9VN8YXZ9VN8YXZ9VNu7QXZ9VN8YXZ9VN8YXZ9VN8YXZ9VN8Y"
```

### JWT Token
```
Header: {
  "alg": "HS256",
  "typ": "JWT"
}

Payload: {
  "id": 1,
  "role": "admin",
  "iat": 1673548800,
  "exp": 1674153600
}

Signature: HMACSHA256(
  base64UrlEncode(header) + "." + base64UrlEncode(payload),
  SECRET_KEY
)

Result: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6MSwicm9sZSI6ImFkbWluIn0.SflKxw..."
```

## 📡 API Communication Flow

```
Frontend                    Backend                     Database
   │                           │                            │
   │  Login Request            │                            │
   ├──────────────────────────>│                            │
   │  POST /api/auth/login     │                            │
   │  {email, password}        │                            │
   │                           │  Query User                │
   │                           ├───────────────────────────>│
   │                           │  SELECT * FROM users       │
   │                           │  WHERE email = ?           │
   │                           │                            │
   │                           │<───────────────────────────┤
   │                           │  Return user data          │
   │                           │                            │
   │                           │  Verify password           │
   │                           │  Generate JWT              │
   │                           │                            │
   │<──────────────────────────┤                            │
   │  {token, user}            │                            │
   │                           │                            │
   │  Store token in           │                            │
   │  localStorage             │                            │
   │                           │                            │
   │  Get Profile Request      │                            │
   ├──────────────────────────>│                            │
   │  GET /api/auth/profile    │                            │
   │  Auth: Bearer token       │                            │
   │                           │  Verify JWT                │
   │                           │  Extract user ID           │
   │                           │                            │
   │                           │  Get user details          │
   │                           ├───────────────────────────>│
   │                           │  SELECT * FROM users       │
   │                           │  WHERE id = ?              │
   │                           │                            │
   │                           │<───────────────────────────┤
   │                           │  Return user               │
   │                           │                            │
   │<──────────────────────────┤                            │
   │  {user: {...}}            │                            │
   │                           │                            │
```

## 🎯 Quick Reference

### Check if User is Logged In
```javascript
if (auth.isAuthenticated()) {
    console.log('User is logged in');
    console.log('User data:', auth.getUser());
}
```

### Make Authenticated Request
```javascript
// Token is automatically added by api.js
const data = await API.get('/files');
```

### Manual Token Management
```javascript
// Get token
const token = localStorage.getItem('token');

// Remove token (logout)
localStorage.removeItem('token');
localStorage.removeItem('user');
```

### Check User Role
```javascript
if (auth.isAdmin()) {
    console.log('User is an admin');
}

if (auth.hasRole('user')) {
    console.log('User has user role');
}
```

## 📝 Summary

Your system is **NOT using Firebase**. Instead:

✅ **Frontend** stores login token in browser localStorage  
✅ **Backend** verifies tokens and processes requests  
✅ **MySQL Database** stores all user data permanently  
✅ **JWT Tokens** handle authentication securely  
✅ **bcrypt** encrypts passwords before storage  

Everything is connected and working! 🎉
